[magento2]
user = vagrant
group = vagrant

listen = /var/run/php7-magento2-fpm.sock
listen.owner = www-data
listen.group = www-data
listen.allowed_clients = 127.0.0.1

pm = ondemand
pm.max_children = 15
pm.max_requests = 500
pm.process_idle_timeout = 10s
